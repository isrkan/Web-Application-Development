[project]
name = "request-handling"
version = "1.0.0"
description = "Request handling with FastAPI - demonstrating path parameters, query parameters, and request bodies"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
]
