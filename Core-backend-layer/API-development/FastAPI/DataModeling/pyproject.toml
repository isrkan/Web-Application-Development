[project]
name = "data-modeling"
version = "1.0.0"
description = "Data modeling with FastAPI and Pydantic - demonstrating type-safe models, validation, and field constraints"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "pydantic>=2.5.0",
]
